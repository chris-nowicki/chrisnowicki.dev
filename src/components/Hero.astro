---
import UnifiedLink from './UnifiedLink.astro'
import ArrowSquiggle from '@/assets/icons/arrow-squiggle.svg'
import Image from 'astro/components/Image.astro'
import Me from '@/assets/images/me.jpg'
import { socialLinks } from '@/lib/site'
---

<section
  class="relative mt-8 pb-4 flex flex-wrap-reverse rounded-xl text-base sm:border sm:text-lg md:mt-30 md:mb-30">
  <div class="p-0 sm:w-2/3 sm:p-8">
    <h1 class="text-3xl sm:text-4xl">
      <span class="font-bold">Hello</span>, I'm Chris Nowicki
    </h1>
    <p class="mt-3 text-lg sm:text-xl">
      Developer Experience Engineer at
      <UnifiedLink
        href="https://www.commerce.com"
        ariaLabel="Visit the Commerce website"
        target="_blank"
        >Commerce.
      </UnifiedLink>
    </p>

    <div class="mt-6 space-y-6 sm:space-y-8">
      <p>
        When I'm not making developers' lives easier with docs and dev tools,
        you'll find me tinkering with new tech, optimizing my productivity setup
        (<em>while keeping it minimal</em>), or hunting for the perfect
        cheeseburger.
      </p>

      <blockquote class="font-cursive relative text-2xl">
        <span class="relative inline-flex items-center">
          Oh, and coffee. Lots of coffee.
          <ArrowSquiggle
            width={45}
            height={45}
            class="arrow-sm-only absolute top-4 -right-12 hidden rotate-90 transform md:hidden"
          />

          <ArrowSquiggle
            width={45}
            height={45}
            class="absolute top-4 -right-12 bottom-0 hidden rotate-90 transform md:block"
          />
        </span>
      </blockquote>
    </div>

    <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:gap-4">
      <UnifiedLink
        href="/contact"
        size="lg"
        variant="default"
        class="bg-black text-center uppercase">Contact Me</UnifiedLink
      >
      <UnifiedLink
        href="https://buymeacoffee.com/chrisnowicki"
        variant="outline"
        target="_blank"
        size="lg"
        class="text-center uppercase">Buy Me A Coffee</UnifiedLink
      >
    </div>
  </div>
  <div class="mb-6 flex flex-col items-center pt-6 sm:mb-0 sm:w-1/3">
    <Image
      src={Me}
      alt="Photo of me"
      loading="eager"
      width={250}
      height={250}
      class="aspect-square w-[200px] rounded-full border-8 border-white object-cover shadow-xl grayscale transition-all duration-200 hover:grayscale-0 sm:w-[250px]"
    />
  </div>
</section>
