<a
  href="/"
  aria-label="Chris Nowicki — home"
  class="wordmark font-cursive inline-flex w-44 items-center text-4xl leading-none">
  <span aria-hidden="true" class="inline-flex items-center">
    C<span class="wordmark-reveal">hris</span>
    <span class="wordmark-reveal">&nbsp;</span>N
    <span class="wordmark-reveal">owicki</span>
  </span>
</a>
<style>
  .wordmark-reveal {
    max-width: 0;
    overflow: hidden;
    white-space: nowrap;
    transition: max-width 240ms cubic-bezier(0.77, 0, 0.175, 1);
  }
  .wordmark:focus-visible .wordmark-reveal {
    max-width: 120px;
    transition: none;
  }
  @media (hover: hover) and (pointer: fine) {
    .wordmark:hover .wordmark-reveal {
      max-width: 120px;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .wordmark-reveal {
      transition: none;
    }
  }
</style>
