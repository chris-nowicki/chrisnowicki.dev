---
import DeskSetup from '@/assets/images/desk-setup.webp'
import Container from '@/components/Container.astro'
import UnifiedLink from '@/components/UnifiedLink.astro'
import PageLayout from '@/layouts/PageLayout.astro'
import { USES } from '@/lib/site'
import generateOgImageUrl from '@/utils/og-image'
import { Picture } from 'astro:assets'
import { softwareData } from '@/data/uses'

const ogImageUrl = generateOgImageUrl({
  header: `/${USES.TITLE.toUpperCase()}`,
  description: USES.DESCRIPTION,
})
---

<PageLayout
  title={USES.TITLE}
  description={USES.DESCRIPTION}
  image={ogImageUrl}>
  <Container>
    <section class="text-md relative pt-20 sm:text-base">
      <header class="mb-8">
        <h1 class="mb-4 text-2xl font-bold sm:text-4xl">Uses</h1>
        <p class="mb-4">
          Being a total tech nerd 🤓, I am always trying new technologies /
          products! Below is a list of all the the HW / SW I use on a daily
          basis. Please note that some of the links are affiliate links.
        </p>
        <p class="mb-4">
          I also have my{' '}
          <UnifiedLink
            href="https://github.com/chris-nowicki/dotfiles"
            target="_blank"
            ariaLabel="Visit my dotfiles repository on github"
            >dotfiles
          </UnifiedLink>

          {' and '}
          <UnifiedLink
            href="https://github.com/chris-nowicki/mac-setup"
            target="_blank"
            ariaLabel="Visit my mac setup repository on github"
            >mac-setup
          </UnifiedLink>
          repositories that go into more depth on the tools I use.
        </p>
      </header>

      <Picture
        src={DeskSetup}
        width={768}
        height={700 * 0.75}
        class="aspect-video w-full rounded-lg"
        alt="Desk Setup"
      />

      <div
        class="text-md relative mt-10 mb-12 flex flex-wrap justify-center gap-4 py-2 font-mono uppercase">
        <div
          class="absolute top-0 right-0 left-0 h-px bg-gradient-to-r from-transparent via-gray-300 to-transparent">
        </div>
        <div
          class="absolute right-0 bottom-0 left-0 h-px bg-gradient-to-r from-transparent via-gray-300 to-transparent">
        </div>
        SOFTWARE
      </div>

      <div class="grid grid-cols-3 gap-5 font-mono sm:grid-cols-6 sm:gap-x-10">
        {
          softwareData.map((software) => (
            <a
              class="group flex flex-col items-center justify-center gap-2"
              href={software.link}
              target="_blank"
              rel="noopener noreferrer">
              <h2 class="text-md group-hover:text-blue-600">
                {software.title}
              </h2>
              <div class="software-icon flex aspect-square w-full items-center justify-center border">
                <img
                  src={software.imageSrc}
                  alt={software.title}
                  width={32}
                  height={32}
                  class="object-cover grayscale transition-all duration-300 group-hover:grayscale-0 group-hover:scale-150"
                />
              </div>
            </a>
          ))
        }
      </div>
    </section>
  </Container>
</PageLayout>

<style>
  .software-icon {
    border-radius: 0.5rem;
    transition:
      border-radius 0.3s ease-in-out,
      transform 0.3s ease-in-out,
      box-shadow 0.3s ease-in-out;
  }

  .group:hover .software-icon {
    border-radius: 50%;
    box-shadow:
      0 10px 15px -3px rgb(0 0 0 / 0.1),
      0 4px 6px -4px rgb(0 0 0 / 0.1);
  }
</style>
