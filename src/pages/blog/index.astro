---
import Container from '@/components/Container.astro'
import HoverList from "@/components/HoverList.astro";
import PageHeader from '@/components/PageHeader.astro'
import PageLayout from '@/layouts/PageLayout.astro'
import { BLOG } from '@/lib/site'
import { getBlogPosts } from '@/utils/blog-helpers'
import generateOgImageUrl from '@/utils/og-image'
import { readingTime } from '@/utils/reading-time'
import { formatDate } from '@/utils/utils'

const posts = await getBlogPosts()

const ogImageUrl = generateOgImageUrl({
  header: `/${BLOG.TITLE.toUpperCase()}`,
  description: BLOG.DESCRIPTION,
})
---

<PageLayout
  title={BLOG.TITLE}
  description={BLOG.DESCRIPTION}
  image={ogImageUrl}>
  <Container>
    <section class="relative pt-12 text-sm sm:pt-20">
      <PageHeader title={BLOG.TITLE} subtitle={BLOG.DESCRIPTION} />

      {
        posts.length > 0 ?
          <HoverList>
            {posts.map((post) => (
              <a
                href={`/blog/${post.id}`}
                data-hover-row
                class="bento-action group block rounded-lg p-4 focus-visible:bg-muted">
                <div class="text-md flex flex-col gap-2 sm:flex-row sm:items-center">
                  <span class="order-1 shrink-0 whitespace-nowrap text-muted-foreground sm:order-3">
                    {formatDate(post.data.date.toDateString())}
                  </span>
                  <span
                    class="order-3 hidden h-px min-w-4 flex-1 self-center bg-muted-foreground/20 sm:order-2 sm:block"
                    aria-hidden="true"
                  />
                  <h2 class="order-2 min-w-0 break-words text-lg font-semibold sm:order-1">
                    {post.data.title}
                  </h2>
                </div>
                <p class="text-muted-foreground">{post.data.description}</p>
                <div class="mt-1 font-mono text-sm">
                  <span class="text-sm tracking-tight text-muted-foreground">
                    {readingTime(post.rendered?.html ?? '')}
                  </span>
                </div>
              </a>
            ))}
          </HoverList>
        : <p class="text-muted-foreground">No blog posts found.</p>
      }
    </section>
  </Container>
</PageLayout>
